# Task-07 visual inspection

Reviewed the actual local Chromium captures, not mockups. QA saves are explicitly self-reported developer test artifacts; they are never installed as student progress. All reference deck pages cited here use physical PDF numbering.

- The desktop network inspector and recorded update show actual vectors, selection-time Q/probabilities, exploration draws, replay member IDs, detached targets, mean batch loss, gradients, parameter-change norms and target-copy counts. Numerical values are consistent with the saved run; display rounding is declared.
- The 390-pixel inspector remains readable: architecture pills wrap, metrics form a compact grid, explanatory text wraps, and the state vector table fits. Final captures also exist at 320 and 768 pixels; automated viewport/axe checks passed. These captures are emulated viewports, not physical-device tests.
- Source/Observe and mathematics captures distinguish the actual part-2 richer-prediction deck from the part-1 replay/target bridge. The GVF and categorical calculations are explicitly supplied examples, not trained distributional outputs.
- The actual comparison table shows seed-dependent outcomes, including a low-loss agent with poor greedy evaluation. No variant is visually predeclared the winner. Loss, training returns and held-out returns have separate plots and numerical tables.
- Initial review found coordinate labels oversized for the fixed canvas margin on the six-cell map. The shared renderer now uses 12px coordinate labels; the final map screenshot has no clipped coordinate text. The loss curve now uses an explicit blue stroke instead of Chart.js's faint default line.
- The final failure fixture displays the excessive-Q explanation, retained last finite parameters and zero successful optimizer steps. Its no-loss metric now reads “Stopped,” not “Warmup.” The test confirms refresh persistence, no completion and real reset-and-train recovery. This is deliberately injected diagnostic evidence, not a naturally failing benchmark run.
- The final inspector says “current experiment,” covering both fresh training and restored checkpoints accurately. New-experiment initialization is described separately from current learned weights.
- Focused keyboard review used actual Step, Pause and Cancel. The final recorded pause/cancel completions were approximately 34/38 ms for that run; all raw timestamps are retained. They are observations, not guaranteed latency bounds.

Inspected files: `lesson-09-network-inspector.png`, `lesson-09-actual-neural-update.png`, `deep-inspector-390.png`, `deep-source-and-observe.png`, `deep-mathematics-focused.png`, `deep-measured-stability-table.png`, `deep-map-and-recorded-update.png`, `deep-live-loss-plot.png`, `deep-training-evaluation-plots.png`, and `deep-failure-fault-injection.png`. Final full-page desktop, completion and mobile-comparison captures are available in `evidence/task-07/screenshots/`; the prior lesson regression captures are not all newly manually reviewed. Human instructor visual/content approval and screen-reader user review remain pending.

# Task 11 audit and disposition of earlier open items

The complete unresolved/remaining sections of TASK_STATUS.md tasks 00–10 were read. Historical milestone statements remain intact; this disposition describes the current product. Technical evidence is in RELEASE_REPORT.md and evidence/task-11. Human approval cannot be supplied by an automated test.

| Earlier open item | Current disposition |
|---|---|
| Engine, algorithms, worker training/evaluation, lessons 04–11 previously out of scope | Implemented by their subsequent milestones; exact numerical fixtures and full lesson browser journeys are rerun for release. All eleven routes remain exposed. |
| Full Sandbox, Compare, Notebook and capstone previously unfinished | Implemented in task 09; release exercises scenario round trips, independent comparisons, archive migration and three expedition protocols. |
| Lecture mode, service worker, offline readiness and updates | Implemented in task 10; release reruns full-cache/offline neural execution, quota/eviction repair, scope and update-preservation cases. External PDFs are deliberately not cached. |
| Occasional tabular cancellation above 250 ms | Task 11 removes repeated cloning of unrelated archived trajectories. Five measured visible cancellations were 32.9, 34.6, 39.1, 43.4 and 50.5 ms. Prior misses remain in task-10 evidence. |
| Prior unexplained 507.4 ms background timer gap | Not reproduced in the task-11 finite run (23 ms maximum). Its historical cause is unresolved; no universal latency or long-duration guarantee is made. |
| Main bundle above Vite's 500 kB warning | Still a build warning: approximately 520 kB minified. Lazy neural execution remains separate. No warning threshold was raised. Loading and responsiveness were measured; this is a size limitation, not a fabricated failed network asset. |
| Native 200%/400% zoom formerly unrun | Task 11 reran actual Chromium tab zoom: seven manual/math/lecture view combinations passed with no document overflow or axe violations. Native screen-reader speech/navigation and physical touch devices remain unrun; automated axe, keyboard and touch emulation are narrower evidence. |
| Fresh dependency installation formerly unrun | Release installs the lockfile into an isolated disposable directory and rebuilds there. Initial default-cache permission failure and retry evidence are retained. This preserves the user's working installation. |
| Detailed source mapping and lesson-02 mismatch | Still unresolved. Eleven local PDF hashes match the original successful retrieval. Linked 2.pdf covers exploration/exploitation; the explicit requested MDP scope remains visible and must be approved or corrected by the instructor. |
| Lesson-10 and lesson-11 source scope | Original live activities are functional and honestly labeled. Part-2 richer prediction methods and IRL/RLHF are conceptual coverage, not implemented algorithms. Instructor approval of scope and activity mappings is still required. |
| Instructor brand/content review and student pilot | NOT PROVIDED / NOT RUN. Automated authoring review, numerical fixtures and QA completion are not human signoff or student learning evidence. This blocks an approved full-course teaching release. |
| Public/protected course catalog | Protected catalog remains unrefreshed; verified sanitized first-match lesson order is preserved. No course password or complete catalog is shipped. Initial restricted requests failed and the web reader could not open three PDFs. Subsequent authorized read-only requests returned HTTP 200 for the homepage and all eleven PDFs, with hashes identical to the verified references. |
| Firefox/WebKit/Safari, physical mobile, GPU, throttled network, long-duration whole-process memory | NOT RUN. Supported verification scope is local Chromium, CPU workers and emulated responsive input. GPU backends and cross-GPU bitwise identity are not offered. |
| Unbounded histories / archive pruning / future migrations | Current finite limits and explicit errors remain. General laboratory records can be exported/deleted; older lesson histories have smaller bounds. Supported legacy formats migrate; unknown future schemas are rejected while existing data is preserved. No silent eviction, cross-device sync or authenticated grading. |
| Active refresh can lose work since last checkpoint | Still explicit: latest consistent delivered checkpoint is recoverable; unfinished comparison orchestration remains cancelled/incomplete. No hidden background trainer. Export before closing a storage-denied tab. |
| Optional learned stochastic/nonstationary model, multi-agent extension, extra battery/time missions | Not part of the agreed required algorithm/lesson scope. Engine battery/time/consumed inventory are tested; Dyna declares deterministic stationary support; invalid combinations are rejected. No enabled feature is silently omitted from task state. |
| Transfer/zero-shot formerly unrun | Task 09 implements/tests separate known-map, compatible local-goal adaptation and zero-shot protocols. Coordinate-only tables, actors and the current coordinate DQN cannot pass unsupported new-map transfer tests. |
| Integration/deployment | A complete archive and isolated placement instructions are produced. No push, publish, host homepage change or live hosting test is authorized or performed. |

## Mathematical and terminology review

| Area | Implementation reviewed and independent evidence |
|---|---|
| Return and planning | First reward exponent zero; [0,0,5] at .9 gives 4.05; the no-cost 5-versus-delayed-20 switch is (1/4)^(1/3). Synchronous expectation/optimality backups, merged collisions, residual versus value error, finite/absorbing gamma=1 restrictions; planning unit fixtures. |
| Sampled prediction/control | First-visit complete-return MC keeps a fixed policy and skips incomplete returns. TD target 4.6/new V 2.26; Q target 8.78/new Q 3.686; SARSA target 5.9/new Q 3.398; true terminal target 5/new Q 3.308. External caps preserve the defined bootstrap. Fixed prediction policy is not control. |
| Approximation and Dyna | Explicit informative/aliased feature dimensions and actual semi-gradient weights. Learned tuples come from real samples; planning uses its own RNG and counter. One real alpha-.5 reward-2 backup gives 1, one simulated backup gives 1.5. Equal real budgets are separate from total update counts. |
| Policy gradients | Stable max-shift softmax, categorical sampling, score onehot-minus-probability. Objective is fixed-start discounted return; actor increments include outer gamma^t. REINFORCE uses complete suffix returns and the declared frozen episode baseline; actor-critic treats TD signal as fixed. Finite differences and exact update fixtures test the convention. |
| DQN | Vector observations, actual small MLP and SGD, detached half-squared TD loss, bounded replay and optimizer-step target copies. Terminal masking and external-cap bootstrap are distinct. Finite gradients, target synchronization, save/load, evaluation isolation and disposal are tested. Loss is not task success or proof of convergence. |
| Imitation | Pre-action state/observation aligns with its action label. Full-batch cross-entropy gradient uses training trajectories only. Uniform four-action alpha-.5 single-pair update gives [-.125,.375,-.125,-.125]. Copy compatible actor preferences only; critic/RNG/counters start fresh. Held-out likelihood and task returns remain separate. |
| Evidence and calibration | Existing calibrated runs are indexed with hashes in calibration-registry.json. Foundations use route/residual boundaries; later lesson challenges combine real comparison completion with concepts and measured winner/tie recognition. Expedition requires 600 source interactions and 4/5 frozen successes plus concepts on the declared guided protocol. Arbitrary edited-map scores are not comparable grades. |

These are code/content consistency checks supported by numerical fixtures. They do not replace independent instructor review of explanations, curriculum scope, difficulty, or pedagogy. No new slide-page citations were assigned.

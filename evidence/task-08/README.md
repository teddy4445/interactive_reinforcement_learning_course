# Task-08 evidence

These are disposable **developer QA artifacts**, not student progress, authenticated grades, or claimed human learning results. Numerical data were generated by the implemented algorithms and recorded browser actions. Nothing in this directory is automatically loaded into a fresh production course session.

- `manual-qa-demonstrations.json`: lower-path keyboard recording and upper-path touch-button recording, with exact engine traces, seeds and declared train/held-out split.
- `browser-qa-imitation.json`: genuine completed lesson-11 QA activity record, fitted policy and ten RL checkpoint/evaluation records, alongside unchanged imported task-07 records for lessons 04–10. Standalone demonstrations are included too; predictions and reflections explicitly say QA.
- `comparison-summary.json`: actual 600-new-interaction browser comparison, full fitting history, frozen cloned baseline and raw per-agent evaluations.
- `verification-summary.json`: compact arithmetic summaries of that recorded comparison. Its observed outcome is not a universal performance claim.
- `eleven-route-audit.json`: actual titles, six stage labels and initial incomplete status for every lesson route.
- `visual-review.json`, `screenshots/`: focused final-layout captures and responsive measurements; see `docs/IMITATION_VISUAL_REVIEW.md` for screenshots actually inspected.
- `source-review.json`: verified local PDF hash/selected visual anchors; unsuccessful fresh web-reader and restricted Node refresh. See `docs/IMITATION_REFERENCE_AUDIT.md` for exact gaps.
- `lint.log`, `typecheck.log`, `unit.log`, `build.log`, browser logs and asset hash manifests: executed checks, not assumed results.
- Other `browser-qa-*.json` and screenshots in this directory are regenerated earlier-lesson regressions. Earlier task directories remain intact as historical evidence and legacy-import inputs.

Failure history is retained. `browser-imitation.log` had five passes and two phone-overflow failures; `browser-imitation-final.log` passed all seven after constraining the layout. `unit-initial-timeout.log` had 262 passes and a 5.355s legacy-backup validation exceeding its original 5s test timeout. A bounded 15s timeout for that large import fixture yields 263 passing tests in `unit.log`. `visual-capture-initial-failure.log` was a screenshot-harness setup mistake: injecting a QA save after the app had read storage, without reloading. The capture script now reloads before opening the lesson; `visual-capture.log` completed successfully.

Not run / unavailable: successful public-deck refresh (web reader inaccessible; Node fetch EACCES), full activity-to-slide/instructor approval, real-student usability calibration, physical touch/screen-reader testing, Firefox/Safari, offline/service-worker release verification, fresh dependency installation/audit (no dependency changes), deployment/host integration. No live ACML site, hosting settings or remote repository was changed.

Final regression history: `browser-full.log` ran all then-current 82 scenarios: 81 passed and the dual neural-lesson journey exceeded its 240-second per-comparison wait at 9/10 agents. The measured CPU-worker benchmark on this host took 11.3–14.1 seconds per 600-interaction run; no neural numerical assertion failed. The harness now allows 480 seconds per comparison and 1,200 seconds for the dual journey without changing training budgets. `browser-final-affected.log` passed all eight selected scenarios (the complete neural 09/10 journey plus seven imitation cases) in 12.4 minutes.

`unit-final.log` passed 264 tests after the recorder-ID and asynchronous-control fixes. The final import review then added a strict array/object distinction and a regression case. `unit-final-import.log` ran 265 tests with 262 passing and three existing numerical fixtures exceeding their 5-second time limits. That failed run is retained; the subsequent single-worker command reduces contention without weakening numerical assertions. Final results are recorded in TASK_STATUS.md.

The single-worker retry `unit-serial-final.log` passed 264/265 checks; the 15-agent neural comparison took 21.856s against its prior 20s timeout. Its ceiling is now 60s with unchanged numerical assertions/budgets. `unit-serial-verified.log` then passed **265/265**, nine files, in 126.07s. The unit tensor-memory artifact now writes into task-08 evidence. Production build completed in 4.88s; final asset hashes are in `build.json`.

Final affected production browser run: `browser-final-imitation.log` **8/8 PASS**, 5.0 minutes, including the complete lesson-11 journey and the new malformed-array rejection case. Together with the 81 broad-regression passes and the passing dual-neural journey rerun, all **83 distinct scenarios** have passing coverage; a single final 83-test run was not repeated. `build-full.json` and `build-affected.json` identify preceding builds; `build.json` identifies the final import-hardened build. Final lint and strict JSDoc checks also pass.

Both final capture scripts exited successfully. `combined-completion-review.json` records a fresh validated UI import of the real QA records and all eleven completed statuses; the island and notebook screenshots were inspected. See `docs/IMITATION_VISUAL_REVIEW.md` for final inspector and combined-completion review. Task 08 is complete; tasks 09 onward were not started.
